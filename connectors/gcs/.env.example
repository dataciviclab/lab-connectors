# Configurazione opzionale per l'ambiente locale.
# Se usi ADC via `gcloud auth application-default login`, non serve impostare nulla.

# GOOGLE_APPLICATION_CREDENTIALS=C:\percorso\service-account.json
